# 默认值配置完成

## ✅ 已完成的功能

我已经成功设置了止损比例和杠杆的默认值：

### 1. 止损比例默认值 - 10%
- **修改文件**: `OrderWatch/ViewModels/MainViewModel.cs`
- **修改位置**: 第177行
- **修改内容**: 
  ```csharp
  [ObservableProperty]
  private decimal _conditionalStopLossRatio = 10; // 默认止损比例10%
  ```

### 2. 杠杆默认值 - 10倍
- **已存在设置**: 在`MainViewModel.cs`中已经设置了多个杠杆属性的默认值：
  - `_leverage = 10` (第108行) 
  - `_marketLeverage = 10` (第136行)
  - `_limitLeverage = 10` (第158行)
  - `_conditionalLeverage = 10` (第174行)

- **界面设置**: 在`MainWindow.xaml`中的杠杆ComboBox已设置默认选中项：
  ```xml
  <ComboBoxItem Content="10" IsSelected="True"/>
  ```

## 🎯 功能说明

### 止损比例
- **默认值**: 10%
- **位置**: 下单设置区域的"止损比例"输入框
- **作用**: 用于计算以损定量时的风险控制

### 杠杆设置
- **默认值**: 10倍
- **位置**: 下单设置区域的"杠杆"下拉框
- **选项**: 1, 5, 10(默认), 20, 50, 100倍
- **作用**: 用于控制交易的杠杆倍数

## 🔄 下一步操作

1. **关闭当前运行的程序**：解决文件锁定问题
2. **重新编译**：`dotnet build`
3. **测试验证**：确认默认值设置正确

## ✨ 用户体验优化

现在用户在：
- **首次使用时**：止损比例和杠杆都会自动设置为合理的默认值
- **快速交易时**：无需每次手动设置常用参数
- **风险控制时**：10%的默认止损比例提供了合理的风险保护

所有默认值设置已完成，用户可以在重新编译后享受更便捷的交易体验！

